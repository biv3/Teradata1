CREATE SET TABLE TD_BIM_FR_TRNG_DB.NEWEMPTABLE ,FALLBACK ,
     NO BEFORE JOURNAL,
     NO AFTER JOURNAL,
     CHECKSUM = DEFAULT,
     DEFAULT MERGEBLOCKRATIO
     (
      employee_name VARCHAR(30) TITLE 'NAME' CHARACTER SET LATIN  NOT CASESPECIFIC
      ,employee_location VARCHAR(30) TITLE 'Location' CHARACTER SET LATIN NOT CASESPECIFIC
     )
PRIMARY INDEX ( employee_name,employee_location );
